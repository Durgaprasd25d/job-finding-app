// import React, { createContext } from 'react';

// const AuthContext = createContext();

// export default AuthContext; // Exporting as default, assuming only one export is needed

// // Additional logic related to AuthContext if required
